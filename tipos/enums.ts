( () => {

    // let audioLevel = 5

    enum AudioLevel {
        min = 1,
        mid = 5,
        max = 10
    }

    let currentAudio = AudioLevel.mid

    console.log(currentAudio);
    console.log(AudioLevel);
    

})()